{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "globalEnv": ["DATABASE_URL", "DEV", "NODE_ENV", "PRODUCTION_ADMIN_EMAIL", "PRODUCTION_ADMIN_FIREBASE_UID", "PRODUCTION_SEED_CONFIRM", "SHADOW_DATABASE_URL", "STAGING_SEED_CONFIRM"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**", "dist"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "cache": false
    },
    "test:e2e": {
      "dependsOn": ["^build"],
      "cache": false,
      "inputs": ["tests/**", "fixtures/**", "utils/**", "playwright.config.ts"]
    }
  }
}
